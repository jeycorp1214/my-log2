# 기능 추가 평가

> 판단 기준: mylog 핵심 가치 — Local-first, 알림 없는 자발적 기록, 간결한 UI, 20-40대 타겟

---

## ✅ 추가할 만한 것

### 잔디 심기 (기록 히트맵)

GitHub 잔디처럼 날짜별 기록 밀도를 색상으로 표현.

**이유**
- 강압적이지 않음. 알림 없이 성취를 시각화.
- 이미 계획된 "연간 리포트" 기능과 데이터 재사용 가능.
- expo-sqlite 쿼리 하나로 구현 가능. 외부 라이브러리 불필요.
- "오늘의 회상" 진입점으로 활용 가능.

**주의사항**
- 게이미피케이션 요소(점수, 레벨업 등) 배제. 순수 시각화만.
- 설정 탭 또는 연간 리포트 화면에 위치.

---

### 사진 일기 결합 (날짜별 대표 사진)

캘린더 각 날짜에 사진 한 장을 배경으로 설정.

**이유**
- 관계 기록 앱의 핵심 감성 강화. 한 달이 사진 앨범이 됨.
- "데이터 주권" 원칙 부합. 사진도 로컬 저장.
- expo-file-system 이미 설치됨.
- 리텐션 효과: 앱이 추억 보관함이 되어 삭제 저항 높아짐.

**주의사항**
- 로컬 저장 용량 관리 필요. 섬네일 리사이즈 필수 (expo-image-manipulator).
- Google Drive 백업 시 사진 포함 여부 별도 설정 옵션 고려.
- Board 뷰에서만 표시, Compact 뷰에서는 dot만 유지.

---

### 타임라인 뷰 (수직 뷰)

현재 캘린더 뷰 외에 하루 기록을 시간 순으로 수직 나열하는 뷰.

**이유**
- 현재 아코디언 목록의 시각적 업그레이드. 코드 재사용 가능.
- 특정 날짜 탭 시 진입하는 형태로 자연스러운 UX 연결.
- react-native-reanimated 이미 설치됨. 구현 비용 낮음.

**주의사항**
- 별도 탭이 아닌 날짜 셀 탭 → 슬라이드업 시트 형태 추천.
- 시간 단위 일정이 없는 앱이므로 "시간축" 없이 단순 세로 나열.

---

## ❌ 추가하면 안 되는 것

### React Native Paper

**이유:** Gluestack v3 + NativeWind 이미 사용 중. UI 라이브러리 이중 설치 = 번들 증가 + 스타일 충돌 리스크.

---

### react-native-draggable-flatlist

**이유:** 체크리스트 드래그 앤 드롭 용도. mylog에 체크리스트 기능 없음.

---

### date-fns

**이유:** dayjs 이미 설치 및 전역 설정 완료 (`utils/date.ts`). 날짜 라이브러리 이중 사용 불필요.

---

### 루틴 템플릿

**이유:** 체크리스트 세트를 불러오는 기능. mylog는 체크리스트 앱이 아님. 앱 컨셉 외부.

---

### 연속 달성 스트릭 (Streak)

**이유:** 핵심 가치 "알림 없는 자발적 기록"과 직접 충돌. 스트릭은 매일 켜야 한다는 강박을 유발. 타겟 유저(조용히 관리하고 싶은 사람)와 정반대 방향.

---

### 성장형 캐릭터 / 식물 키우기

**이유:** 앱 컨셉(담백한 관계 기록)과 맞지 않음. 간결한 UI 원칙 위반. 구현 비용 대비 컨셉 적합도 낮음.

---

### Time Blocking (드래그 앤 드롭)

**이유:** 체크리스트 → 캘린더 변환 기능. mylog에 체크리스트 없음. 전제 조건이 없어 구현 불가.

---

### '미룬 일' 모아보기 / 칸반 보드

**이유:** 할 일 관리(To-do) 앱 기능. mylog는 기록 앱. 앱 정체성이 흐려짐.

---

### 플로팅 타이머

**이유:** 집중 시간 측정 = 생산성 앱 기능. 관계 기록 앱과 무관. 앱 범위 확장 비용만 발생.

---

## 요약

| 기능 | 판단 | 근거 |
|------|------|------|
| 잔디 심기 | ✅ | 강압 없는 성취 시각화, 연간 리포트 연계 |
| 사진 일기 결합 | ✅ | 관계 기록 감성 강화, 리텐션 최강 |
| 타임라인 뷰 | ✅ | 현재 아코디언 뷰 대체, 구현 비용 낮음 |
| React Native Paper | ❌ | Gluestack 중복 |
| draggable-flatlist | ❌ | 체크리스트 없음 |
| date-fns | ❌ | dayjs 중복 |
| 루틴 템플릿 | ❌ | 체크리스트 앱 아님 |
| 스트릭 | ❌ | 핵심 가치 충돌 |
| 캐릭터/식물 | ❌ | 컨셉 부적합 |
| Time Blocking | ❌ | 전제 조건(체크리스트) 없음 |
| 미룬 일/칸반 | ❌ | To-do 앱 기능 |
| 플로팅 타이머 | ❌ | 생산성 앱 기능 |
